apiVersion: v1
kind: Service
metadata:
  name: car-catalog-service
spec:
  type: NodePort
  selector:
    app: car-catalog-api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
